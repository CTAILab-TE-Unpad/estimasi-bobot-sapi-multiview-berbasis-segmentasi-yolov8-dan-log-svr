# =============================================================================
# requirements/dev.txt
# Development and testing dependencies. NOT included in the Docker image.
# Install with: pip install -r requirements/base.txt -r requirements/dev.txt
# =============================================================================

-r base.txt

# --- Testing ---
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.25.0          # required by FastAPI TestClient for async support

# --- Linting & Formatting ---
ruff>=0.1.0            # fast linter + formatter (replaces flake8, isort, black)

# --- Type Checking ---
mypy>=1.7.0
types-Pillow
pandas-stubs
